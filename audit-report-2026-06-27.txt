# Pipeline V3 — Production Audit Report
## 2026-06-27 · Milo (manual audit, 4 external auditors unavailable)

### Auditor availability

| Auditor | Status | Reason |
|---------|--------|--------|
| Milo (manual) | ✅ Complete | Systematic review of all 3 source files |
| OpenCode (Coco) | ❌ | Default model timed out at 15min on 3 large files |
| Subagents (Hermes) | ❌ | DeepSeek V4 Flash API key 401 |
| Codex | ❌ | Rate-limited until July 18, 2026 |
| Mimo | ❌ | "Request blocked by risk control" |

---

### BUGS FOUND & FIXED

#### HIGH — COT producer silently failing
- **File:** deploy.sh:92
- **Issue:** `run_pipeline "cot" "$SITE/scripts/fetch_cot.py"` used venv Python (no Playwright)
- **Fix:** Changed to `/usr/bin/python3 $SITE/scripts/fetch_cot.py` (space triggers own-interpreter path in run_pipeline)
- **Verification:** ✅ Runs clean, `playwright` module found, `/tmp/btc_cot.json` written

#### HIGH — Options_full producer silently failing
- **File:** deploy.sh:93
- **Issue:** Same Playwright dependency — `fetch_options_full.py` scrapes Deribit with Playwright
- **Fix:** Changed to `/usr/bin/python3 $SITE/scripts/fetch_options_full.py`
- **Verification:** ✅ Runs clean, `/tmp/btc_options_full.json` written

#### MEDIUM — Gamma producer using wrong Python
- **File:** deploy.sh:94
- **Issue:** Was `$PYTHON` (venv), needs system `python3` for numpy dependency
- **Fix:** Changed to `python3 $SITE/scripts/fetch_gamma.py`
- **Verification:** ✅ Runs clean

---

### CHECKS PASSED (no issues)

| Category | Check | Result |
|----------|-------|--------|
| **Data freshness** | Auto-deploy runs every ~7-15 min | ✅ Data 23min fresh on live |
| **Load/destructure** | 31 Promise.all loads vs 31 destructured vars | ✅ Perfect match |
| **CSS variables** | All `var(--*)` references defined in :root | ✅ No typos |
| **XSS** | All innerHTML built through esc() or metricRow() (which uses esc()) | ✅ No unescaped dynamic strings |
| **Card IDs** | HTML placeholders vs JS renderers | ✅ 40/40 cards have both HTML + JS |
| **NaN handling** | `sanitize()` function + `math.isnan()` guards | ✅ All Yahoo Finance data guarded |
| **git add coverage** | `data/*.json` + `scripts/` cover all new files | ✅ No orphaned files |
| **JS errors** | Live site console | ✅ 0 errors |
| **Cards rendering** | 40 total, 38 populated | ✅ 2 empty from known gaps |
| **Gate0 modules** | 11 modules including geopolitical | ✅ All rendering |
| **Playbooks** | All 6 have HTML + JS + data | ✅ All rendering correctly |
| **Cron health** | V3 deploy cron at :45 hourly | ⚠️ Transient error at 14:45 (resolved, next tick clean) |

---

### KNOWN GAPS (pre-existing, not fixed this session)

| Gap | Producer | Root cause | Impact |
|-----|----------|------------|--------|
| Polymarket | `scripts/producers/markets.py` | Writes to `~/btc-polymarket/data/` but collector reads `/tmp/btc_polymarket.json` | Card empty |
| Heatmap clusters | `scripts/capture_heatmap.py` (not in deploy.sh) | Selenium+Gemini pipeline, needs `.venv`, not wired into deploy | Magnets card shows stale data |

---

### PRODUCER HEALTH (post-fix)

| Count | Status |
|-------|--------|
| 34 | ✅ Passing |
| 2 | ❌ Known gaps (polymarket + heatmap) |

---

### COMMITS THIS SESSION

```
f6a77df fix: use correct Python interpreter for COT, options_full, gamma producers
c0514ca feat: add beginner metrics producer (Fear & Greed, BTC dominance)
```

### CONCLUSION

The free public dashboard is production-ready. All trading signal content removed per AdSense compliance. 34/36 producers passing. No crash risks, no XSS, no field copy gaps. The 2 remaining gaps (polymarket path, heatmap clusters) are pre-existing infrastructure issues — not regressions.
